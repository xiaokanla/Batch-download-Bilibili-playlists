# 更新日志

本文件记录面向用户的主要变化。版本发布时，将“未发布”内容移动到对应版本号下。

## 未发布

## v1.4.6 - 2026-09-25

### 优化

- 新增保守型下载加速：API 直链模式下并行下载视频流和音频流，但总连接数仍限制为 8，批量视频继续串行处理。
- 优先使用播放接口返回的常规 UPOS CDN，当前线路失败时自动尝试同一响应中的备用地址，不增加 B 站 API 请求。
- Aria2 下载增加断点续传参数；用户设置限速时继续串行下载音视频流，避免并行导致总速率超过限制。
- 直链模式下 Aria2 不兼容或失败时自动切换 yt-dlp 内置下载器，无需手动删除 Aria2。

### 新增

- 新增无损切换 B站账号：新账号扫码确认前保留当前登录状态，切换后按 UID 自动加载对应下载历史。
- 新增明确的 2K/4K 批量下载模式，按 1440P/2160P 优先逐个下载选中视频，并在源视频不支持时降到目标范围内最高可用画质。
- 未登录时使用独立的登录舞台取代空工作区；登录成功后中轴开合，工具栏和视频列表从中心展开。
- 新增可收起的缓存管理面板与 API，可查看各类缓存路径、文件数和占用，并在后台校验迁移程序数据或 Eagle 生成缓存。

### 安全

- 将 yt-dlp 最低版本提升到 `2026.8.19`，纳入 2026 年分片外部下载器相关安全修复。

## v1.4.5 - 2026-09-21

### 优化

- 主工作区控制区域支持上下拖动调节高度、按钮收起/展开，以及双击拖动条恢复自适应；列表已有内容时会自动压缩控制区，显示更多视频。
- 新增浅色主题，可在顶部一键切换深浅外观，并自动记住上次选择。
- 视频列表页码支持直接输入并跳转，自动限制有效范围并在首尾页禁用对应翻页按钮。
- 扩展视频右键菜单，新增单视频下载、选择切换、下载状态切换，以及复制链接和标题等快捷操作。
- 重做收藏夹选择器，支持名称搜索、总数提示、清晰的列表标识与选中状态，并扩大下拉面板的可读区域。
- 统一升级时长、年份、标签范围、清晰度及 Eagle 等全部下拉菜单，补充一致的主题样式、键盘操作和长列表搜索。
- 右侧功能面板支持独立收起与展开并记忆状态；环境诊断、标签分析、Eagle 和日志默认收起，任务运行时自动展开相关面板。

## v1.4.4 - 2026-09-20

### 新增

- 新增独立的 Web UI 动效模块 `webui/motion.js`，与下载、同步和 Eagle 业务逻辑分离。
- 主界面加入分区错峰入场、模式切换、数值变化、弹层和提示消息动效。
- 同步、下载、标签生成和 Eagle 导入运行时显示进度流光、按钮掠光和面板状态呼吸。

### 优化

- 视频卡片选择改为局部同步更新，不再等待全列表统计和状态扫描；点击后立即显示选中状态。
- Ctrl+A 支持全选/取消全选切换，并新增方向键、Space、Enter、Ctrl+F、Esc 等列表快捷操作。
- 修复账号投稿搜索遇到空响应或非 JSON 风控页时显示 `Expecting value` 的问题，并支持失败时降级使用旧缓存。
- 重构账号投稿工作流：与收藏夹操作区分展示，账号搜索移入主工作区，候选账号改为包含头像、名称、粉丝数和 UID 的可视化选择卡片。
- 视频列表仅在数据签名变化时重新生成，轮询更新不会反复播放卡片入场动画。
- 动效主要使用 `transform` 与 `opacity`，空闲状态只保留少量低成本动画。
- 支持系统“减少动态效果”设置，会自动关闭持续动画并缩短过渡。
- 保持可拖动三栏布局以及窄窗口单列布局，不增加横向滚动。

## v1.4.3

- 下载后低频缓存 B 站标签，并支持 Eagle 历史视频标签修复。
- Eagle 导入可追加视频标签并维护独立标签组。
- 改进 Eagle 标签关系分析、智能收藏夹筛选和标签管理。
- 修复 Aria2 环境诊断及无 FFprobe 时的音轨检查回退。
